# Python build artifacts / caches
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
*.egg

# Compiled emitter build products (regenerated by the CMake build; the wheel
# packages the .so via package-data, so it never needs to be tracked in git)
*.o
*.so
